# Downloaded model weights (large; fetched via envs/download_models.py)
models/

# Nsight Systems traces and logs are large and should not go through GitHub.
kernel_launch/**/results/_work/
kernel_launch/**/results/nsys/
kernel_launch/**/logs/
*.log
nohup.out
*.nsys-rep
*.nsys-rep.gz
*.sqlite
*.sqlite.gz
*.qdrep

# Python / OS
__pycache__/
*.pyc
.DS_Store

# Metrics JSON/CSV are intentionally not ignored; they are small enough to keep
# in GitHub as the durable profiling summaries.

# Local scratch reports/plots.
kernel_launch/figs/
kernel_launch/report.md
kernel_launch/report*.md
old_qwen3_5/

# Task-graph runs keep compact JSON/TXT/CSV reports, but generated CUDA,
# launchers, and logs are local H100 artifacts.
task_graph/results/**/raw/
task_graph/results/**/run.log
