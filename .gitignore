# Downloaded model weights (large; fetched via envs/download_models.py)
models/

# Transient raw nsys artifacts (compressed copies under results/nsys/ are kept).
kernel_launch/**/results/_work/
*.nsys-rep
*.sqlite

# Python / OS
__pycache__/
*.pyc
.DS_Store

# NOTE: compressed nsys results (results/nsys/**/*.gz), metrics, and logs ARE
# tracked and pushed to GitHub -- the instance is ephemeral, so GitHub is the
# durable store for the profiling outputs.

# rsync partial/temp files
kernel_launch/**/results/nsys/**/.*.gz.*
kernel_launch/**/results/nsys/**/.rsync-partial/
